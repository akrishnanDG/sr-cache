apiVersion: v1
kind: Service
metadata:
  name: {{ include "sr-cache.fullname" . }}
  labels: {{- include "sr-cache.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "sr-cache.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
