# Apply real values out-of-band; this file is a template only.
# kubectl create secret generic sr-cache-upstream \
#   --from-literal=UPSTREAM_URL=https://psrc-xxxxxxx.region.aws.confluent.cloud \
#   --from-literal=UPSTREAM_API_KEY=... \
#   --from-literal=UPSTREAM_API_SECRET=...
apiVersion: v1
kind: Secret
metadata:
  name: sr-cache-upstream
type: Opaque
stringData:
  UPSTREAM_URL: "https://psrc-xxxxxxx.region.aws.confluent.cloud"
  UPSTREAM_API_KEY: "REPLACE_ME"
  UPSTREAM_API_SECRET: "REPLACE_ME"
